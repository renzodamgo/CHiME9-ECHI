name: baseline

args:
  inference_dir: ${enhancement_dir}
  config_path: data/working_dir/experiments/ha-joint18/train_ha/hydra/.hydra/config.yaml
  ckpt_path: data/working_dir/experiments/ha-joint18/train_ha/checkpoints/ha-joint18_024.pt
  audio_device: ${device}
  window_size: 60
  stride: 56
  # Force use of joint model architecture
  model_override:
    name: joint
